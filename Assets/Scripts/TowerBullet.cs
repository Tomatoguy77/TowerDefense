using UnityEngine;
using System.Collections;

public class TowerBullet : MonoBehaviour
{/*
    void Update()
    {
        ShootBullet();
    }

    void ShootBullet()
    {
        GameObject ZuessTower = GameObject.Find("Tower");
        Tower towerScript = ZuessTower.GetComponent<Tower>();
        this.transform.position = Vector2.MoveTowards(new Vector2(transform.position.x, transform.position.y), towerScript._checkForEnemy.gameObject.transform.position, 3 * Time.deltaTime);
        if (transform.position == towerScript._checkForEnemy.gameObject.transform.position)
        {
            Destroy(this.gameObject);
        }
    }*/
}
